server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sebsatian/PrestaBanco_TINGESOEV2
          default-label: main
          search-paths: config-data
  application:
    name: config-server
